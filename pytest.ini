[pytest]
testpaths = engines model-layer storage desktop-shell
addopts = --import-mode=importlib -m "not live"
# Live tests are deselected by default (E1); run them explicitly:
#   python -m pytest -m live
markers =
    live: requires LM Studio / local services running (run explicitly with -m live)
