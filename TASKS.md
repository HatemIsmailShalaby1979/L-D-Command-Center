> **Internal development artifact** — documents the AI-assisted build process for this project.

# TASKS.md

Shared task queue for multi-agent coordination. Status values: OPEN, CLAIMED:<agent>, DONE:<agent>, BLOCKED:<reason>.
See `BOOT_ROOT.md` — Multi-Agent Coordination Protocol.

DONE:opencode: model-layer client for LM Studio's local API with schema-validated responses and retry-on-failure
DONE:opencode: podcast/audiobook length compliance — word-budget prompt + make_length_validator (MIN_CONTENT_WPM×tolerance floor bound to requested minutes) + controller stretch re-render (≤0.70×, if actual <92% of target) + audiobook duration excludes 44-byte WAV header; +8 tests; suite 1058 passed / 7 deselected; also fixed TestGenerateJourneyFlow pollution of generate_journey (monkeypatch.setattr)
DONE:claude-code: journey-core HTML card generator for a single topic+level (renderer module + generator)
DONE:claude-code: export-engine plain-text and PDF output from a journey

DONE:claude-code: career-engine resume schema and generation with model-layer retry pattern
DONE:claude-code: wire Resume objects into export-engine (PDF + DOCX support), add career-engine integration test covering generate→enhance→export→GitHub→LinkedIn→YouTube (all mocked)
DONE:claude-code: add TTS client to model-layer with Piper (default) and Kokoro-82M (optional) backends, backend-agnostic synthesize() interface

--- Production plan queue (IDs are canonical — see /docs/PRODUCTION_PLAN.md; review report: /tmp/architecture-review-20260824-094204.html) ---

DONE:opencode: P0.0 git initialized on main, baseline commit 09681c3, tagged audit-2026-08-24; secrets/OS cruft verified excluded (D1 resolved)
DONE:opencode: P0.1 root conftest.py (hyphen-dir alias packages) + pytest.ini (--import-mode=importlib); all ~30 sys.path.insert sites removed across 20 files; schema collision resolved via canonical dotted imports; suite green from root
DONE:opencode: P0.2 all 8 previously-failing tests now pass under pinned deps; old failures not reproducible post-migration (likely fpdf2 API drift + env drift); permanent guard = pinned requirements.txt + one-command suite. NOTE: 5 live-skips confirm audit defects #1/#2 in un-mocked paths — closed by P1.5/P2.1
DONE:opencode: P0.3 deleted all __pycache__/ and .pytest_cache/ from tree before baseline commit
DONE:opencode: P0.4 requirements.txt pinned (httpx 0.28.1, fpdf2 2.8.8, python-docx 1.2.0, pdfplumber 0.11.10, requests 2.32.5, pytest 9.1.1, reportlab 5.0.1; piper-tts optional)
DONE:opencode: P1.1 build Generation Pipeline deep module in model-layer (render→call→extract→validate→feedback-retry→transient-retry→typed errors); single model-id policy; register youtube summary templates
DONE:opencode: P1.2 one fake-client suite proving pipeline contract + live-marker smoke tests (skipped when LM Studio absent)
DONE:opencode: P1.3 journey-core generate_journey migrated onto Pipeline (public interface unchanged); verified via run_checks.sh
DONE:opencode: P1.4 career-engine generate()/enhance() on Pipeline; Enhancement changes list survives retries (envelope validated per-attempt); validator now schema-driven via _validate_object — dead contact required-check gone (defects #4/#6 closed)
DONE:opencode: P1.5 migrate podcast_script + bilingual generation onto Pipeline; both broken _call_model copies DELETED (defect #1 closed); their suites inject scripted clients through the public seam; live-guard tests in export-integration now use the real error taxonomy
DONE:opencode: P1.6 YouTube summarization on Pipeline; templates registered; validate_video_summary + feedback retry replace best-effort parsing (malformed/empty now raise SchemaValidationError after retries)
DONE:opencode: P2.1 KOKORO_IMPLEMENTED gate in tts.py; narration auto-select defaults to Piper until Kokoro exists (defect #2 closed); explicit overrides honored; flag-flip test keeps future behavior covered
DONE:opencode: P2.2 WAV check fixed to [8:12]; real-shaped-WAV regression test added (defect #3 closed)
DONE:opencode: P2.3 Voice Catalog module (engines/audio-engine/voice_catalog.py): narrator/pair/speaker resolution + has_language provisioning check; 16 tests consolidating narration/podcast/bilingual voice tables into one (language, role) → voice map
DONE:opencode: P2.4 assembly.py render_segments seam live; narration/podcast/bilingual consume it + Voice Catalog; cross-engine private imports eliminated render_segments([(text, voice, speed)]) → AudioResult; migrate all four audio consumers; language-lab stops importing underscore-privates
DONE:opencode: P2.5 provisioning.py — voice_urls/missing_voices/download_voice + README checklist; ja_JP-style ids handled; 9 tests (downloader/checklist + missing-voices error listing exactly what to fetch)
DONE:opencode: P3.1 podcast templates interpolate {language}/{level}/{host_name}; immersion forwards target_language+level (defect #5 CLOSED); LANGUAGE_NAMES in Voice Catalog
DONE:opencode: P3.2 bilingual verification pass — bilingual_verify template, validate_verdict, verify_bilingual_pair + generate_bilingual_pair_verified regenerate-on-fail loop with full verdict audit trail
DONE:opencode: P4.1 export god-module split into detect/text/pdf/docx/pptx/xlsx adapters behind thin export() dispatch
DONE:opencode: P4.2 generation removed from export: podcast/bilingual/immersion kinds raise ValueError pointing at explicit engine composition; narration stays (deterministic text->audio via assembly seam)
DONE:opencode: P4.3 PPTX + XLSX Journey exporters added (python-pptx 1.0.2, openpyxl 3.1.5 pinned)
DONE:opencode: P4.4 byte-stability suite green: pdf/pptx/xlsx/docx pinned metadata => identical bytes on repeat exports (E4 satisfied)
DONE:opencode: P5.1 storage engine v1: persistence.py (artifact kinds + preferences, LDCC_DATA_DIR) and secrets.py (single KEY=VALUE adapter); github/linkedin/youtube loaders now delegate parsing to it, keeping only local validity policies; stale E:/ paths cleaned
DONE:opencode: P5.2 desktop shell v0 (vertical slice): tested ShellController + thin Tkinter window (journey generate→render→save→export txt/pdf/pptx/xlsx + health indicator). FOLLOW-UP: resume/language-lab tabs + library browser
DONE:opencode: P5.3 typed errors surfaced: FlowResult envelope maps ConnectionError/ApiError/SchemaValidationError/ValueError/unexpected to dialog kinds with actionable details; 6 dedicated tests
DONE:opencode: P5.4 ldcc.spec v2 verified ON LINUX (2026-08-25): spec now self-stages hyphen dirs into importable packages + enumerates all lazy imports; Tcl/Tk 9 libs collected for uv standalone python; frozen Storage root honors LDCC_DATA_DIR/APPDATA/XDG instead of _MEIPASS; dist/ldcc smoke-ran 25s clean under DISPLAY=:0 against live LM Studio — caught first real GUI bug (ttk.Label fg=) since shell was never launched before. Windows build still pending (needs PyInstaller run on Windows side).
DONE:opencode: P6.1/P6.2/P6.4 error-sweep+gates+docs: run_checks.sh gate (syntax+suite+cov floor 90%), live deselected by default, immersion prints->logger, shell logging config, root README fresh-machine guide, ship-gate snapshot in plan §8


--- Phase 7 queue: Paradise Playground + Language Lab power-up (see /docs/PLAYGROUND_AND_LANGUAGE_LAB_PLAN.md; owner amended vision 2026-08-24) ---

DONE:opencode: P7.1 model-layer/capabilities.py — six task profiles (min-size + review note; bilingual keeps mandatory verify <14B), best-effort size-from-id estimator, one-shot probe through Pipeline (max_attempts=1, no feedback retries), verdict persisted via new storage kind "capabilities" + preference pointer; client.list_models() added; controller run_capability_probe/capability_summary; health bar shows "ready | model (~NB): ready/degraded — …" with Probe button; 27 tests incl. validator rejection branches
DONE:opencode: P7.2 LessonPack schema + Pipeline templates (lesson_pack_generate/lesson_pack_retry) + generate_lesson_pack() with feedback retry — one validated pack: two-voice dialogue (exactly 2 distinct speakers, feeds two-voice rendering), vocab_cards, grammar_cards with drills, evaluation[] in 3 grader-friendly types (multiple_choice/fill_in_blank w/ ___ marker/translation)
DONE:opencode: P7.3 deterministic graders (exact/normalized/accent-folded/slash-alternatives for fill-in-blank+drills+transformation rewrites; MC integer-index checks; regex keys explicitly deferred — decision recorded after review found original wording silently dropped it) + model-judge fallback for free-form answers (lesson_judge, inspectable GradeResult w/ canonical answer+issues) + pack fidelity audit (claim-level verdict artifacts under storage "verdicts"); transformation evaluation type added P7.2 stack-wide post-review
DONE:opencode: P7.4 LanguageLabRenderer — interactive HTML pack: flip/self-grade vocab flashcards, grammar drills graded in-browser by JS mirroring graders.py normalization (edge-punct incl ¿¡, accent-fold, slash alternatives), MC/fill-in-blank evaluation, translation items honest self-grade vs reference, listening items wired to segment audio via dialogue-N/listening-N artifact names (consumed by P7.5), final score breakdown; deterministic output; all model content escaped
DONE:opencode: P7.5 per-segment audio via assembly.render_segments — render_pack_audio() emits one WAV per dialogue turn named per the renderer contract (<stem>-dialogue-<i>.wav); speakers resolved to distinct language-scoped voices through the Voice Catalog; optional output_path persistence; mp3 off by default (browser plays wav)
DONE:opencode: P7.6 spaced-repetition-lite (SM-2) — pure scheduler (ladder/ease-floor/lapse rules, injectable today) + SrsStore persisted through Storage preferences blob; due-card selection and cross-restart persistence tested
DONE:opencode: P7.7 Media Workspace core — engines/playground-bridge/media_workspace.py: pure MediaSpec planners (convert/trim/scale/pad/volume/mix/concat/overlay) + injectable-runner execute + ffprobe probe + collision-safe ingest; 18 tests, no ffmpeg required; conftest gains playground_bridge alias
DONE:opencode: P7.8 storage media/* kinds + Import Inbox — persistence.py accepts namespaced "media/<subkind>" (validated); scan_inbox() one-shot watch-folder moves drops into storage with collision-safe names, ext filter, delete-after semantics, per-failure isolation; shell timer drives it in P7.12
DONE:opencode: P7.9 Connector Hub seam + gradio_client adapter — connectors_hub.py (Connector ABC capabilities/send/poll, failures-as-data) + connectors_gradio.py (pinned keyless HF Spaces: FLUX image gen, ACE-Step music; injectable client factory keeps gradio_client optional)
DONE:opencode: P7.10 Figma REST adapter — connectors_figma.py on the Hub seam: free-account FIGMA_TOKEN via secrets seam, two-leg export (images endpoint → asset bytes), png/svg + scale, list_frames() picker, injectable transport; failures are actionable failed Jobs
DONE:opencode: P7.11 Pollinations keyless image adapter — connectors_pollinations.py on the Hub seam: zero-account GET generation (urlencoded prompt, width/height/model/seed/nologo), content-type guard, injectable transport, failures-as-data
DONE:opencode: P7.12 Playground tab — controller playground seam (connector roster keyless-first with quota-note payloads, run_connector_job → media/generated, import_files + scan_import_inbox, media list/load, new "connector" error kind) + thin Playground tab (canvas list over library/generated/inbox, file-picker import + inbox folder drop path, connector panel rendering quota notes, prompt Generate)

--- Career agent: job-board search + application packages (owner-directed 2026-08-25) ---

DONE:opencode: engines/career-engine/job_boards.py — keyless connectors for Greenhouse (v1 boards-api), Ashby, RemoteOK; unified JobListing dataclass; keyword matching (title×3 + snippet×1 scoring); cross-source URL dedupe; dead-source isolation (logged, never fatal)
DONE:opencode: cover letter generation — cover_letter_generate prompt template + generate_cover_letter() in career generator; grounded 150-250 word letters mentioning company; pipeline-validated
DONE:opencode: controller agent flows — search_jobs_now (keyword filter, score-ranked top-30), check_job_watchlist (seen-URL diff, 10min auto-check), save_job_watchlist (persist role/filters/seen-set), prepare_application (enhance + cover letter + PDF/DOCX + listing.txt), upload_resume (PDF/DOCX/TXT + confidence flags), import_github_projects (repos → projects), fetch_linkedin_profile (name/email → contact)
DONE:opencode: Career tab UI expansion — resume preview pane, upload button (filedialog), connections row (GitHub username + Import + LinkedIn), Job Search section (role/location fields + Treeview + Prepare application), Watchlist section (save/check/auto-check toggle every 10 min)
DONE:opencode: PDF Unicode fix — _UnicodeFPDF mixin auto-swaps Helvetica for DejaVu (Linux) or Arial (Windows) TTF; regression-tested with em-dash/curly-quotes crash from live use
DONE:opencode: live smoke — Greenhouse search verified against real network (162 Stripe listings, keyword matching returning ranked results)

--- Phase 8: Stabilization & Polish (owner-directed 2026-08-26) ---

DONE:claude-code: P8.1 — log Phase 8 transition, update MASTER_STORY.md with D9 (hardening phase entered, no new features until Phase 8 closes)
DONE:claude-code: P8.2 — audit journey-core generation quality across varied topics/levels
DONE:claude-code: P8.3 — audit Language Lab (flagship) quality: LessonPack generation, grading, rendering, audio
DONE:claude-code: P8.4 — audit career-engine live flow: resume generation, enhancement, job search, application packages
DONE:claude-code: P8.5 — audit desktop shell UI/UX: tab layouts, loading indicators, error messages, consistency
DONE:claude-code: P8.6 — triage all audit findings into prioritized fix list (STABILIZATION_FIXLIST.md), append as P8.7+ tasks
DONE:claude-code: P8.7a — [CRITICAL] Diagnose and fix journey-core live generation: confirm loaded model (qwen-2.5-14b working), prompt template matches schema (no changes needed), live generation 100% success rate across 3+ topics, conftest alias fix resolved 78 test failures
DONE:claude-code: P8.7b — [HIGH] Live-test Language Lab: generate_lesson_pack() tested across 3 language pairs (English→Spanish Latin, English→Japanese non-Latin, English→Arabic non-Latin) — all 3 passed with 6 dialogues + 6 vocab cards each
DONE:claude-code: P8.7c — [HIGH] Make bilingual verification mandatory — confirmed unverified generate_bilingual_pair() exists but is NOT called by UI (controller.py has NO bilingual calls); UI only uses generate_lesson_pack() for Language Lab; verified path generate_bilingual_pair_verified() and convenience generate_and_render() exist but are internal only
DONE:claude-code: P8.8 — loading indicators: buttons disabled during generation, status text updated
DONE:claude-code: P8.9 — user-friendly errors: added ERROR_ACTIONS mapping with actionable guidance for each error kind
DONE:claude-code: P8.12 — document watchlist format: added detailed docstring to storage/persistence.py set_preference explaining job_watch schema
DONE:claude-code: P8.13 — spacing and layout consistency: license bar and quota badges follow the same ttk.Frame + tk.Label pattern as the rest of the shell
DONE:claude-code: P8.14 — clear/reset buttons added to Journey (Topic), Language Lab (Topic), and Career (profile text) tabs
DONE:claude-code: P8.15 — dark theme applied (TLabelFrame included); Save-as… buttons added to Journey, Language Lab, and Audio Studio tabs; fixed NameError crash (undefined `_save_as_html`) and local variable shadowing in do_audiobook()/do_podcast()

--- Career agent: configurable company rosters (owner-directed 2026-09-05) ---

DONE:opencode: configurable job-board rosters — engine-level contact-center/CX defaults (HEAD-probe verified 2026-09-05: legacy stripe/figma roster fully dead; new GH dialpad/five9/nextiva/vonage/intercom/qualtrics/hubspot/cresta/observeai/maestroqa, Lever aircall, Ashby helpscout/gorgias/kustomer/glia), job_sources preference + save_job_sources, three-tier roster resolution (explicit arg > saved preference > engine default) in search_jobs_now/check_job_watchlist/save_job_watchlist, UI roster fields (3 boards, comma-separated tokens) with Save rosters, watchlist legacy-shape fallback, 6 new tests (736 passing)

--- 2026-09-05 owner directive: kill the 12B/14B format wall, freeze Study-Studio-owned features, focus Language Lab + Career + Playground ---

DONE:opencode: F1 — hardened JSON extraction (model-layer/schema.py): noise/BOM strip, MMdd/thinking-block removal (closed + unclosed-with-JSON-after semantics), fence stripping, Python-literal fix outside strings, string-aware balanced-span scan (braces inside strings no longer break extraction), repair_truncated_json salvage for finish_reason=length outputs; 22 new tests (test_extraction.py)
DONE:opencode: F2 — pipeline hardening (model-layer/pipeline.py): DEFAULT_MAX_TOKENS 4096→8192, truncation detection + repair + "token limit" actionable feedback, response_format json_object with runtime-rejection fallback (attempt not consumed), JSON-discipline addendum + size-aware attempt budget (<7B gets 5) via new model-layer/policy.py (estimate_model_size moved there; capabilities re-exports), client timeout 120→300s, lesson-pack budget 4096→8192, resume budget 2048→4096; 13 new pipeline tests
DONE:opencode: F3 — career identity memory (desktop-shell/controller.py): career_identity preference (github_username/projects, linkedin basics, target_role) persisted by import_github_projects/fetch_linkedin_profile/enhance_resume; offline cache serves previously imported data when GitHub/LinkedIn unreachable; actionable errors for 404-username, rate-limit-token, missing-LINKEDIN_TOKEN; get/clear_career_identity flows; linkedin_posts storage kind
DONE:opencode: F4 — LinkedIn post studio: linkedin_post_generate/retry templates (anti-AI-tell style rules), generate_linkedin_post() with deterministic validation (30-260 words, banned phrases, ≤2 hashtags), controller draft_linkedin_post (saves locally) + publish_linkedin_post (explicit confirm gate — nothing posts without it); career tab UI: goal field, Draft, editable preview, Publish… with yes/no dialog
DONE:opencode: F5 — UI focus rework (desktop-shell/app.py): tab order Language Lab → Career → Playground → frozen Journey/Audio Studio (banners point to Study Studio, generate buttons disabled, exports still work); async runner (worker thread + queue + 100ms Tk polling — window never freezes during any generation/search/connector job); Language Lab: 15 languages with names, saved-packs reopen list; Career: identity restore on boot, cached/offline badges, search/prepare/generate/enhance/connections all async with busy buttons; boot smoke verified against dead LM Studio (graceful red health bar)
DONE:opencode: F6 — docs/PROFIT_PLAN.md: monetization strategy (tier ladder Free/Pro $9/Campaign $29, offline license keys, shareable-pack growth loop, 5 revenue lines ranked by stability, 90-day ladder, non-goals, risk register)
DONE:claude-code: F7 — license-key seam + Pro gates UI + model quickstart + shareable-pack button + README storefront:
  (a) License panel in desktop shell (app.py): tier badge, quota display (packs/week, application packages, resume profiles, LinkedIn posts), activate-key dialog (simpledialog paste), deactivate, upgrade link — all offline reads through controller.license_status() -> LicenseStore.summary();
  (b) Quota badges: Language Lab shows "Packs: N/3 left this week" next to Generate button, refreshed after each generation; Career tab refreshes license after resume generate, application prepare, and LinkedIn draft;
  (c) Model quickstart hint (PROFIT_PLAN §8 risk #1): when LM Studio unreachable/no model loaded, health bar shows curated quickstart_hint() from model-layer/quickstart.py — recommends Qwen 2.5 7B Instruct by default, filters by RAM; no 4GB weights bundle needed;
  (d) Share/export-pack button in Language Lab history section: calls controller.share_lesson_pack() -> self-contained HTML with app credit footer (PROFIT_PLAN §3.1 growth loop); free on every tier;
  (e) README rewritten as storefront: comparison table (L&D vs Duolingo/Babbel/LinkedIn Premium), pricing tier table, model recommendation table, distribution strategy, design principles;
  (f) index.html landing page updated with pricing table and comparison table;
  (g) ERROR_TITLES/ACTIONS updated with "license" error kind;
  851 tests still passing
DONE:claude-code: P8.13 — spacing and layout consistency: license bar added between header and tabs with consistent padding; quota badges use same gray fg as other status labels

--- PROFIT_PLAN execution (owner-directed 2026-09-06) ---

DONE:opencode: G0 � review of the uncommitted F7 follow-up work (license seam storage/licensing.py + 30 tests, quickstart catalog, share footer in renderer, README/index storefront, app license bar): verified design (offline HMAC tokens, honour-system by design, campaign_mode tier feature), confirmed 851 suite green; FIXED boot regression � claude-code inserted lab_quota_label + Share button ABOVE their parent widgets (lab_actions/history_box created later), app crashed at startup with "lab_actions referenced before assignment"; reordered Language Lab block (actions frame -> quota badge -> share wiring) and re-verified boot smoke + suite
DONE:opencode: G1 � flagship-pack catalog (engines/language-lab/flagship_packs.py + tests): 10 curated topics per focus language (es/ja/de) chosen for conversion (renting an apartment, job interview, first week at work, customer-support call, doctor visit, grocery small talk, public transport, banking bureaucracy, making friends, salary negotiation); stable keys for permanent share links; localized notes (Anmeldung/keigo/Castilian); 7 catalog tests
DONE:opencode: G2 � flagship batch generator (engines/language-lab/flagship_batch.py + 5 tests): idempotent one-shot CLI producing the 30 footered marketing artifacts (python -m engines.language_lab.flagship_batch; --limit smoke mode; per-topic failure isolation; skips existing so shared links never drift) � PROFIT_PLAN section 6 Days 31-60 delivered as tooling
DONE:opencode: G3 � Campaign mode v0 (engines/career-engine/campaign.py + 13 tests): status ladder (prepared/submitted/screening/interviewing/offer/rejected/withdrawn), CampaignStore persisted through Storage preferences (URL-keyed dedupe, re-prepare never resets history, notes with timestamps, summary counts + open pipeline); interview_prep_generate/retry templates; generate_interview_prep grounded in the real resume (company/role echo validated, 5 questions + 3 stories + 3 questions-to-ask exact counts)
DONE:opencode: G4 � Campaign wired into app: controller campaign_status/advance/track/untrack/generate_interview_prep flows (prepare_application now auto-tracks every package; tracking + history free on every tier per PROFIT_PLAN section 7, advance + prep gated to campaign_mode); Career tab campaign panel (table, refresh, track-from-search, advance dropdown, interview prep renders into the preview pane); interview_preps storage kind; 6 controller tier-gating tests � PROFIT_PLAN section 6 Days 61-90 v0 complete
DONE:codex: G5 repo-side launch kit — docs/LAUNCH_KIT.md contains the five-minute demo script, founder-key offer, outreach copy, and 25-seat pilot checklist. External execution remains owner-owned: checkout account, recording/upload, recipient selection, and sending outreach.

--- Project E.T. � Language Lab expansion (owner-directed 2026-09-06/07) ---

DONE:opencode: L1 � voice foundation: mic.py (sounddevice capture: device autodetect, push-to-stop + sample-count silence autostop, live RMS pulse callback, typed NoMicrophone/DeviceBusy errors with typing-mode copy � UI callback failure never kills capture), stt.py (faster-whisper lazy singleton, RAM-aware tiny/small int8, one-time download into models/stt with disk guard, TranscriptResult with clarity/WPM, empty-audio kindness, typed SttUnavailableError), voice pairs (PIPER_LANGUAGE_VOICE_PAIRS + et_voice_pair real/fallback flag), assembly.pitch_shift (resample-based, clamped �5 st); requirements pinned sounddevice==0.5.6 faster-whisper==1.2.1 after LIVE verify on CPython 3.10.11/Windows (tiny model downloaded+loaded 25.7s, 5s audio transcribed 0.4s); 33 new tests (test_mic/test_stt/test_et_voices)
DONE:opencode: L2 � E.T. core: et_persona.py (Mr./Mrs. personas, system_persona tone rules incl. banned AI-tells, curated ENCOURAGEMENTS bank en/es/de/ja, localized STATUS_LINES ladder), et_scenarios.py (30 everyday scenarios across 10 domains + mock-interview/support-call/free-talk specials, 3 lengths), et_conversation.py (session state machine: voice+text turns, transcribe->evaluate->reply->speak->persist pipeline, clarity-floor re-ask that never consumes a turn, turn limits, persistence round-trip), et_reply/et_evaluate prompts; FREE_QUOTAS et_turn 10/week; controller E.T. surface (scenario bank, mic devices, start/opening/submit_voice+text/end/state/replay; quota gate BEFORE recording; transcript persisted every turn); 39+ tests (test_et_core, test_controller_et)
DONE:opencode: L3 � E.T. UI: et_ui.py panel in Language Lab (persona/scenario/length/mic pickers, record button with live pulse + push-stop, typing fallback always visible, chat bubbles with score chips + corrections + praise, status ladder listening->transcribing->thinking->speaking->saved, replay-last, quota badge, encouragement line per turn); _open_audio daemon-thread player; device/et_repeat error kinds; boot smoke green with 9 real mic devices + LM Studio reachable
DONE:opencode: L4 � curriculum library: cefr.py (A1..C1 + honest locked C2 badge, 10 everyday domains, 6 deterministic slots/level with concrete curated titles, grammar spine per level, catalog_index zero-LLM for all 15 languages), catalog_store.py (progress: todo/generated/done, done-never-downgraded, level %, next-lesson pointer, restart-surviving), controller curriculum flows (catalog/stats/next/generate_slot gated BEFORE LLM with auto-registration + audio + audit, mark_done), lab_ui.py library panel (level cards grid with progress, slot list with status/score, generate/open/share buttons, continue-learning card); 28 new tests
DONE:opencode: L5 � exams + placement: level_exam.py (6-section inclusive exam schema: 8 vocab MC, 10 grammar 6mc+4fill, reading 60+ word passage + 5q, writing task, listening 6-8 turn two-speaker script + 5q, speaking repeat/describe/respond; deterministic objective grading with tiny-typo forgiveness; exam_judge rubric with neutral-3 graceful fallback; repeat-task STT similarity; SECTION_WEIGHTS sum 100, PASS_THRESHOLD 70; result with per-skill bands + recommendations pointing at real curriculum slots + warm verdict lines), placement.py (12-item escalating A1->B1 quiz, honest banding, recommended_first_slot), reading/writing/grammar/speaking/judge/placement prompts; controller level_exam_start (1 shot/level free, Pro retakes; listening WAV rendered via assembly) / submit (grades all six, persists result, pass marks level slots done) / results; placement_start (never gated) / grade / last; exams_ui.py panel (placement card, exam tabbed notebook all six sections, mic-capture speaking option, full result render); 34 new tests
DONE:opencode: L6 � Skills Arena: skills.py (document text extraction txt/PDF/DOCX never-raises, reading packs with glossary+4q validation, writing evaluation with corrected-version + 2-5 highlights + preserve-their-voice rules), reading_pack/writing_eval prompts + retry; controller skills flows (reading_from_file -> footed HTML pack, writing_submit gated 3/week + saved record, listening_from_file -> STT transcript + questions pack); skills_ui.py arena in Playground (reading import, writing prompt bank + eval, listening import, speaking door to E.T.; media canvas + connectors untouched); _render_reading_pack_html deterministic renderer; 9 new tests
DONE:opencode: L7/L8 � packaging + governance: ldcc.spec hiddenimports for the voice stack, assets/ + E.T. face README, README capabilities + E.T./STT install notes, PROFIT_PLAN section 10 (E.T. as the Pro headline; free-tier taste quotas), storage kinds (level_exams, placement, et_sessions), governance files updated. Suite: 1032 passed, 7 deselected (baseline 885 -> 1032, +147 tests). Boot smoke: RUN-OK with real devices + live server.
BLOCKED:owner: LIVE smoke with a loaded model — this machine CLI model load is blocked by a Vulkan UUID bug. Run `python -m pytest -m live` after loading a model through the LM Studio GUI.
DONE:codex: production verification pass — pinned soundfile and pytest-cov; fixed desktop_shell alias and corrupted initializer; added coverage gate config; compiled 211 source files; rebuilt Windows dist/ldcc.exe with CPython 3.10/PyInstaller 6.22.2.
DONE:opencode: E5 Windows leg — dist/ldcc.exe crash fixed and rebuilt (2026-09-13). The shipped Window exe died at startup with PyInstaller's "Unhandled exception in script" dialog and no window. Reproduced via the console build (_ldcc_console.spec → dist_console/ldcc_console.exe): traceback showed `ModuleNotFoundError: No module named 'desktop_shell.controller'` at app.py run() — the entry script's `_install_source_aliases()` ran unconditionally and in a frozen build replaced the real packaged `desktop_shell`/`model_layer`/`engines` packages in sys.modules with fake packages pointing at loose files under the onefile extraction dir that PyInstaller never materializes (modules ship inside the PYZ archive). FIX: gate `_install_source_aliases()` behind `if not getattr(sys, "frozen", False)` (desktop_shell/app.py) — frozen apps import the staged underscore-named packages directly; source-tree runs keep the conftest-style aliases. Console build re-verified: boots, registers connectors, detects 13 mic devices, renders the L&D Command Center window, handles offline LM Studio gracefully. Rebuilt dist/ldcc.exe (windowed, 133 MB, PyInstaller 6.22.2) and verified on Windows: window "L&D Command Center" renders, no error dialog, no stderr. Blocking v1-tag E5 status now ✅ (Linux 2026-08-25 + Windows 2026-09-13). Suite baseline re-confirmed green: 1041 passed / 7 deselected.
DONE:opencode: Deployment-pipeline enhancement (deployment-engineer lens, post-E5 2026-09-13) — the Windows release is now a repeatable, gated, verifiable, reversible process instead of one ad-hoc PyInstaller command:
  (1) desktop_shell/verify_build.ps1 — automated windowed-launch smoke gate (EnumWindows P/Invoke, asserts window title, probes all matching processes for error dialogs, exit 0/1/2, LDCC_SKIP_SMOKE hatch); validated: real PASS (~48s boot), INVALID→2, fail paths.
  (2) desktop_shell/check_deployment_policy.py — secrets/artifacts policy gate (staged-tree scan for secrets/ models/ .env *.secrets .onnx .gguf .bin + spec baseline datas=[]/binaries=[]); validated PASS.
  (3) desktop_shell/write_build_manifest.py — sha256/bytes/UTC/toolchain/spec-sha/upstream -> dist/ldcc-build.json + human one-liner ldcc-build.txt; validated.
  (4) desktop_shell/build_release.bat — six fail-fast stages (suite -> policy -> archive previous exe -> PyInstaller -> manifest -> smoke), auto interpreter selection (3.10 w/ PyInstaller first), --no-gate/--no-policy/--no-verify. Validated end-to-end: RELEASE OK.
  (5) desktop_shell/rollback_release.bat — restore newest dist/archive\ldcc-<ts>.exe (+ fragment filter). Validation CAUGHT a real bug: `for /f` overwrote BEST per iteration so the FIRST build was restored instead of the newest; fixed via `if not defined BEST` guard, re-validated (restores newest, hash-matched).
  (6) CI: fixed broken compile step in python-app.yml AND pylint.yml (`desktop-shell` → `desktop_shell`, the path that never existed); new windows-build.yml runs all six stages on windows-latest/3.10 + uploads dist artifact (60-day); new release.yml (workflow_dispatch only, `environment: release` approval gate) builds and publishes a GitHub Release with exe + manifest. CI not run locally (not a git repo) — activates on GitHub push.
  (7) docs/DEPLOYMENT_PIPELINE.md (stages, usage, rollback runbook, verification protocol, monitoring, secrets policy, CI behavior); README Building Executable now points at the gated pipeline.
  Governance updated: TASKS.md, AGENT_LOG.md, FILE_MANIFEST.md, CONTEXT.md.

---
UPDATE 2026-09-23 — Endpoint auto-detect (ollama/LM Studio) applied; quality guard (non-robotic + humor/tips) active; e2e smoke report: E2E_SMOKE_REPORT.md. Release judgment: small boring change shipped; rollback via previous archive in build/.
DONE:opencode: 2026-09-23 session — auto-detect endpoint (client.py scan_local_endpoints), quality guard (model-layer/quality_guard.py), frozen-state verified, smoke_headless.py 19/0, rebuild started, docs updated (28 .md + 4 .html), branch merged to main (3075a7a).
DONE:opencode: LIVE desktop run verified — TCLLIBRARY repaired, run_now.py window active, ldcc.exe PID 14416 started/stopped clean.
BLOCKED:owner (updated 2026-09-23): LIVE smoke with loaded model — CLI model load blocked by Vulkan UUID bug on this host; reproduction done (ollama endpoint reachable, direct call TIMEOUT 60s — data); fix requires driver/model-load environment change, not code change.